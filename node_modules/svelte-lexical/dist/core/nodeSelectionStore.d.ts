/**
 * Ported from lexical/packages/lexical-react/src/useLexicalNodeSelection.ts
 */
import type { LexicalEditor, NodeKey } from 'lexical';
export declare function isNodeSelected(editor: LexicalEditor, key: NodeKey): boolean;
/**
 * Clear editor selection
 */
export declare function clearSelection(editor: LexicalEditor): void;
/**
 * Stores `isSelected` state for a SvelteComponent node.
 * Rather than updating the component state directly, it updates the editor node selection and receives updates from the editor.
 */
export declare function createNodeSelectionStore(editor: LexicalEditor, nodeKey: string): {
    subscribe: (this: void, run: import("svelte/store").Subscriber<boolean>, invalidate?: ((value?: boolean | undefined) => void) | undefined) => import("svelte/store").Unsubscriber;
    set: (selected: boolean) => void;
};
